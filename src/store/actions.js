import * as types from './mutation-type'
import { Auth, List, Task } from '../api'

export default {
  login: ({ commit }) => {},
  fetchLists: ({ commit }) => {},
  addTask: ({ commit }) => {},
  updateTask: ({ commit }) => {},
  removeTask: ({ commit }) => {},
  logout: ({ commit }) => {}
}
